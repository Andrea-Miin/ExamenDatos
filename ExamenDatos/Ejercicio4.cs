using System;

public class Ejercicio4
{
	public Class1()
	{
        List<int> listaMax = new List<int>();
        for (int x = 0; x <= lista.Length; x++)
        {
            listaMax.Add(x);
        }
        var result = listaMax.Max();
        Console.Write("El valor mas alto es: ");
        Console.WriteLine(result);
        Console.ReadLine();
    }
}
